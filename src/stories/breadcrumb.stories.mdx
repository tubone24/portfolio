import { storiesOf } from '@storybook/react';
import { withInfo } from '@storybook/addon-info';
import { withKnobs, text, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import BreadCrumb from '../components/breadcrumb';
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";

<Meta title="Components/BreadCrumb" parameters={{ component: BreadCrumb }} />

ただのパンくずリスト

<Source code={`import BreadCrumb from "~/components/breadcrumb";`} />

# Preview

<Preview withSource="open">
  <Story name="BreadCrumb">
    <BreadCrumb dark={boolean('dark', false)}
    crumbs={[{name: text('name1', 'test'), link: text('link1', '/')}, {name: text('name2', 'test2'), link: text('link2', '/2')}]} />
  </Story>
</Preview>

# Props

<Props of={BreadCrumb} />
