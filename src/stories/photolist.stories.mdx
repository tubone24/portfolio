import { storiesOf } from '@storybook/react';
import { withInfo } from '@storybook/addon-info';
import { withKnobs, text, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import PhotoList from '../components/photoList';
import { Meta, Story, Preview, Props, Source } from "@storybook/addon-docs/blocks";
import dedent from 'ts-dedent';

<Meta title="Components/PhotoList" parameters={{ component: PhotoList, backgrounds: { default: 'twitter', values: [ { name: 'twitter', value: '#00aced' }, { name: 'facebook', value: '#3b5998' }, ], }, }} />

カルーセルを使ったフォトリスト

# Preview

<Preview withSource="open">
  <Story name="PhotoList">
    <PhotoList />
  </Story>
</Preview>

# Props

<Props of={PhotoList} />
